# Copy to .env.local for local development.

# Base URL of the chatbot backend (see chatbot-backend/README.md).
# Must be reachable from the browser, so it's exposed via NEXT_PUBLIC_.
# Local dev default matches the backend's default port.
NEXT_PUBLIC_CHATBOT_API_URL=http://localhost:8080
